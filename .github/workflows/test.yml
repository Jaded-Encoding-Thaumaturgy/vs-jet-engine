name: Tests

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }}, VS ${{ matrix.vapoursynth-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        vapoursynth-version: [74, 75, 76, 77]
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras --locked

      - name: Run tests with coverage
        run: uv run --with vapoursynth==${{ matrix.vapoursynth-version }} pytest tests --cov=vsengine --cov-report=xml

      - name: Upload coverage to coveralls
        uses: coverallsapp/github-action@v2.3.7
        with:
          file: coverage.xml
          format: cobertura
          parallel: true
          flag-name: ${{ join(matrix.*, ' - ') }}
          fail-on-error: false

  coverage-finished:
    name: Coverage Finished
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Upload coverage to Coveralls (finish)
        uses: coverallsapp/github-action@v2.3.7
        with:
          parallel-finished: true
          fail-on-error: false
